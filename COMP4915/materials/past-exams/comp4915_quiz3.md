# Comp 4915 Quiz 3 2026

**MULTIPLE CHOICE.** Choose the one alternative that best completes the statement or answers the question.

1) Which file test operator returns true if a file called filename exists.
   - A) -f
   - B) -r
   - C) -e
   - D) -d
   - E) All of the above

2) Which of the bash file test operators returns true if a file exists and is a directory?
   - A) -c
   - B) -a
   - C) -b
   - D) -d
   - E) all of the above

3) Which variable contains the number of positional arguments passed to a script?
   - A) $*
   - B) $&
   - C) $#
   - D) $!
   - E) $$

4) In a bash script, comments can start with which of the following:
   - A) #
   - B) --
   - C) !
   - D) /*
   - E) //

5) In bash, what exit status indicates success?
   - A) anything nonzero, but 1 is the convention.
   - B) 0
   - C) 1
   - D) -1
   - E) anything nonzero.

6) What builtin bash function will read and execute the contents of another script file?
   - A) source
   - B) eval
   - C) read
   - D) execute
   - E) ed

7) Which of the following isn't a valid Unix or Linux shell?
   - A) sh
   - B) tcsh
   - C) ksh
   - D) bash
   - E) jsh

8) What must be done before a shell script can be run by just typing its name?
   - A) It has to be compiled.
   - B) Its permissions must be changed to include executable.
   - C) Its ownership has to be root.
   - D) It must be owned by the person typing is name.
   - E) It must be moved to a special spot in the filesystem.

**TRUE/FALSE.** Choose 'T' if the statement is true and 'F' if the statement is false.

9) Each Linux shell (such as bash, tc shell, z shell) offers a different scripting syntax.
