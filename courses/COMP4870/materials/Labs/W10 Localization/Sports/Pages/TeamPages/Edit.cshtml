@page
@model Sports.Pages.TeamPages.EditModel
@inject Sports.Services.SharedResourceService sharedLocalizer

@{
    ViewData["Title"] = @sharedLocalizer.Get("Edit");
}

<h1>@sharedLocalizer.Get("Edit")</h1>

<h4>@sharedLocalizer.Get("Team") @Model.Team.TeamName</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Team.TeamName" />
            <div class="form-group">
                <label asp-for="Team.City" class="control-label"></label>
                <input asp-for="Team.City" class="form-control" />
                <span asp-validation-for="Team.City" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="@sharedLocalizer.Get("Save")" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">@sharedLocalizer.Get("BackToList")</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
