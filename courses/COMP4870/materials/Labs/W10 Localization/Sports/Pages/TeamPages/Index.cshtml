@page
@model Sports.Pages.TeamPages.IndexModel
@inject Sports.Services.SharedResourceService sharedLocalizer

@{
    ViewData["Title"] = @sharedLocalizer.Get("Index");
}

<h1>@sharedLocalizer.Get("Index")</h1>

<p>
    <a asp-page="Create">@sharedLocalizer.Get("CreateNew")</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @sharedLocalizer.Get("TeamName")
            </th>
            <th>
                @sharedLocalizer.Get("City")
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Team) {
        <tr>
            <td>
                @Html.DisplayFor(model => item.TeamName)
            </td>
            <td>
                @Html.DisplayFor(model => item.City)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.TeamName">@sharedLocalizer.Get("Edit")</a> |
                <a asp-page="./Details" asp-route-id="@item.TeamName">@sharedLocalizer.Get("Details")</a> |
                <a asp-page="./Delete" asp-route-id="@item.TeamName">@sharedLocalizer.Get("Delete")</a>
            </td>
        </tr>
}
    </tbody>
</table>
