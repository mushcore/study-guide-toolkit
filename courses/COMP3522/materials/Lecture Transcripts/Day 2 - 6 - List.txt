 Now that we know all the different operations that you can perform on a sequence type, let's look at a specific one Which is the list So in general a container is a data structure and this is used to pretty much group together A bunch of values together and you can kind of think of it like a compound type and a container contains references to other objects and a list is a specific container Created by surrounding a sequence of variables or literals with square brackets And you can kind of think of it like an array, but Since the list itself is like a class object type It has a whole bunch of useful functions or methods that are available So it's similar, but better than the Java array list and the way that we create a new New list is the following so we have a variable name called my list and Then you have the equal sign and then the way that we Initialize a list is all we have to do is surround our variables with some square brackets So you know as we have the square bracket here square bracket here and then we have our two literals in this case So we have the literal number 10 separated by a comma and then the literal ABC string and this automatically creates a New list with two elements with the first element being 10 and the second element being ABC and as I already mentioned a list item is called an element and a list is ordered in indexed just like arrays in other languages So here if we have a variable name my empty list equals to two empty square brackets then you've created a list with no elements in there and And the really cool part About lists compared to other languages is that a list can have many different types of elements in it You know typically in Java when you create an array you have to say it's an integer array a string array or my custom class array But in Python you don't have to do that a List can contain Strings in custom classes all in the same list So here I'm showing you that I have a list called student data equals to Three different elements in here. We have a string We have actually another list so that we have a separate list in here with Three elements we have an integer integer and a double and then as the third element we have the string P in here So this so this list Contains a string another list and then another string So lists are because lists can contain all these different elements They're really useful reducing for reducing the number of variables in a program since a single list can store an entire collection of related variables And individual list elements can be accessed by their index just as you would arrays in other language So a list index must be an integer So here in this example we are creating our student data with our string Another list and then another string So to access this first element Just as we're used to it the first element is at index zero So we access it by going student Square bracket zero and this will give us the value of our string which is this a zero number and Then the neat thing about lists in Python is that you can use negative indexes So right here if you if we call our list student data and then provide it with the negative one index that actually grabs the last element in our list and if we happen to put negative two that would grab this element right here and Coming going back to the operations that we saw earlier of course you can call the length operation on student data So that pretty much gives you the count of elements in our list So here I'll just show you some code fragments here, but I'll go through the actual code file very soon So you can append a pop remove elements from your list You can of course print your list very easily You can access the elements and delete elements as well You can sort the elements in your list in two different approaches and I'll go through how they're different and Find the number of elements and reverse them and then another interesting Operation right here. So let's check out the code