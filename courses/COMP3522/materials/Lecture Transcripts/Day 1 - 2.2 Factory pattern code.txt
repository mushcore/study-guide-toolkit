 Here we have the code for our UMOD diagram on the right side and I'll try to keep the class diagram itself open up on the left side So we can follow along as we're looking at the code So first of all I want to focus on the user or the product portion of the factory pattern and in this case Our products are going to be our users and as we mentioned earlier the difference between our guest and our member users Is they have different permissions and I'm going to be representing our permissions with the bunch of enums So here we have read like share flag and write as a whole bunch of different enums and in the top level we have our user abstract base class and In our user class we just have a user name which is a string date join, which is the time Permissions which is eventually going to be I think a list of permissions and then a password that's going to be a string Then we have some other helpful methods like authenticate to check if the password and that we enter is matching Get profile that returns the user name and date joined and then just a string Dunder to helpfully print out the user so nothing too crazy here just a bunch of functions to represent a user So that's the top level abstract base class that we have right here Next we're going to implement the guest user down here and in this case all we have is a regular init that takes in a user name and password and then we're just going to pass in the username and password to the super which is going to be the init of the Abstract user class and then the key difference is that guest users don't have the ability to write so in this case we're going to set our permissions to be a tuple of all these different permissions right here read, like share and flag but they're missing the ability to write and then for the Overridden dunders string method We're just going to append the string guest user and then call the supers a string method So this will print out that this person's a guest and then their name and the date that they've joined Next we have the member class so member class inherits from user we see in this diagram So we're focusing on this one and the key difference of the member Class is that they do have the ability to write in our form so all this code is the same as We saw earlier for the guest except that for the permissions They now have the extra permission to also be able to write And then their string method is a little bit different because they print out that their member user Compared to the guest user from before Okay, so all this stuff is probably What you're used to writing in all of your other code So now let's change our focus over to the factory So we're going to be focusing on the factories Over here specifically. Let's look at the user factory first So in the user factory You can see it's really short up here our user factory is just going to be inheriting from the abstract base class and all has Is a create user method and it's just going to return a user type or that's in some tension It's going to return a user type and you see that there's no code in the create user method So very simple there And the next we're going to look at the guest factory So remember that the whole purpose of the guest factory is all it does is it instantiates a guest and then returns it So you see that in the guest you guest user factory we're inheriting from user factory And then we're overriding the create user method and in this case We're still returning a user type But the specific concrete user that we're returning is going to be a guest type. So this guest factory It's going to have a dependency on guest Which is right here and then for the guest because they're a guest they're not going to have a name And they're not going to have a password. So that's why we're passing a none and none here And that's it. That's all there is to the guest factory. All it does is it creates a specific guest user type So now let's move on to the member factory So then in the member factory very much the same concept its purpose is to only instantiate a member In this case, however, for for my specific system a member does have a username and password. So we just have some extra code here to get the The user's name Save them to username and get their password that saves it in the password and then we pass it all this information on into the member class And that's it. So hopefully you see that it's actually pretty simple to create these factory classes All right, so next we have the forum the forum website all the way up here So how does that work? So in my forum all I'm going to have is the name of the forum itself So let's say Jeff's discussion forum and then a list of users and in its initializer I just pass in a name for that forum and then initialize a list of empty users And then I have to have some method so that I can add new users to my forum And this is where we kind of bring a lot of the factory design pattern together So notice here that we have this ad user method And in this ad user method I take an effectory So the cool thing here is I don't know what kind of factory I'm going to get I don't know if I'm going to get a guest factory Or if I'm going to get a member factory From the forum website's perspective So for this from this class forms perspective It just thinks it's getting a generic user factory type So this top level abstract factory type And that's why we have this dependency So this one dependency here That's why we have this dependency because this class forum Is dependent on this abstract user factory type And that is this factory right here So whatever factory we get we don't know which one it is We're just going to blindly call create user on it Whenever we get a factory we're just going to blindly call create user on that factory So in other words this forum website up here Is going to call create user factory And then if we happen to be getting a guest factory It's going to call a create guest factory method there If you happen to be passed in a member factory It calls the member factory And creates a member there So that's a cool part So here is where we happen to create a member or guest And we don't know ahead of time which one we're going to get And then after we create that user Whether it's going to be a member Right here or a guest we don't know But we just get some kind of user and we append it So we add that guest or member Into our list of users So that's where the power of this pattern comes from We're we're getting unknown factories And then we're creating some unknown user ahead of time And then we just append it to ourselves And then we actually run the program That's when we finally determine the concrete versions So again I mentioned this is just part of the kind of magical power of this pattern And hopefully this part makes sense Okay, so that's further looking to how we bring this together So you're probably wondering at some point We have to figure out which specific factory We're going to create Whether it's a guest factory or member factory And that's where a kind of external controller comes into play So here this get user factory function Notice that it's not inside of My form It's actually sitting outside So I'm just going to call this Part of like the main function somewhere So I had this random get user function that's outside of scope Of any classes And its purpose is to return a specific user factory And if you look at this code It's just asking us Welcome Do you want to make an account Or continue as a guest user And I give the user an option if they press one Then they're going to make a proper member account Or they have to enter their username and password And if they press two They're going to log in as a guest And they have no no username and password So here's where we actually choose which factory to create If they press one So we get their choice And if it happens to be one So if choice happens to be one Then we Then we use the member user factory If their choice happens to be two Then we're going to use the guest user factory So this is where we actually save The factory that we're going to use And then whichever factory we get We're going to return an instance of that factory To whoever's calling us And we'll see where we calling this in the main function So here is our main So the main is acting as the controller In the sense that the main is kind of directing What everything is happening So the first thing the main does Is we make an instance of our form And we're just going to call our form a discussion form The next thing it's going to do Is we want to add some users to our form Which is these two functions But notice we're doing something tricky First We don't know ahead of time Which kind of user we want to create We don't know if we want to create a member Or a guest user So we call the get user function Which is the function we just looked at So in the get user function Here We're going to ask the user of our program Are we logging in as a member Or a guest So whether they if they hit one Then we're going to instantiate a member factory And then return it back out And then we pass a member factory here Let's say member Use a factory Kind of like that And then if we happen to choose two Then this is going to pass in a guest user factory Like that Into this add user function So let's try it out And then see if It kind of follow along And then just a reminder that at the very end After we add two users All we're going to do is print out The information for the users So let's run this And see what it looks like Oops, I think I'm running a different program Let's just stop that Okay, so I'm running the program And I'm in the forum and it's asking me Do I want to create a A member account or a guest user account So I press one So at this point in time I'm currently on this line of code It's asking me for my choice So when I hit one and then enter What happens is that Because my choice is equal to one I'm going to get my member user factory Save it into factory class And then return an instance of member user factory So in other words It's instantiating a member user factory And then passing that Into my add user function In my forum So if I go to my add user function in the forum Which is right here My add user function The factory that's currently being passed in Is the concrete member factory Over here So my forum is going to blindly call The create user method For my member factory So in other words we're here We're currently in this member factory We're going to call create user on it And this member factory is going to If we follow this arrow It's going to create a member So we're going to go to create an instance Of our member object here So here we call create user for a factory So if we go to our factory For our member user factory We call this create user And that's why it's currently prompting me To enter a username So I'm going to enter Jeff And it prompts me now for the password So I'm going to say 123 And then enter And that will then return a new Jeff member To my forum So my forum gets a Jeff member here And it depends that Jeff member To the user list In the forum So this list now has a single Jeff member in there And that's how it works And then to the next time Um next time in we're back in Maine So we just finished this line of code right here Now we're executing this line again So it's saying I want to add another user To my forum So then we still do that same controller I get user factory call So we're back in to get fact get user factory And it's printing out the same message Welcome Do you want to log in as the account Or as a guest So this case I want to log in as a guest So I choose to And then hit enter In this case Because it hit two It's going to return a guest user factory So guest user factory gets saved into here We return an instance of get user factory Or guest user factory sorry And then so on this line of code What actually gets returned Is guest user factory like this So guest user factory Is passed into the ad user again in my forum So if you go back to my forum In this case this factory is now Being passed in a concrete guest factory So then this concrete guest factory calls the create user function of my guest factory So if we go into The guest user factory there We're calling this function And if we follow the lines in this dependency arrow We see that our guest factory Along here Is going to create a guest object Which we see right here We are creating a guest object So in the case of the guest object It's going to have no username And no password So That's it for this part We return a guest object Which the forum Receives into this user And then we append a guest object Into the self.users list So now in this self.users list We have a Jeff object And we also have a guest object And that finishes these two lines of code Right here So now we're in our for loop And it's going to look through all the users in my forum And print them out So in this case there's a Jeff user Or Jeff member user And a guest user So that's why in the very end You see when it prints out All this information You see there's a Jeff Member user And a guest And none user So hopefully now that you've kind of seen How this works You kind of understand How the factory panel works So to kind of summarize Of course we have to Kind of construct our classes In this Class diagram format And then by doing that This allows us to have this kind of magical And user function That can accept any kind of factory And we rely on polymorphism To properly choose the correct concrete factory That we're passing in So if we pass in a guest user factory We're going to call the guest user factory Create user If we pass in a member factory It'll call the member factories create user And by doing that This forum class No longer depends So this forum class no longer depends On all these concrete Classes down here And instead it's just depending on these top level Abstract Classes up here