 So the first of all is we want to use it when we have a giant class that we want to be able to divide into separate hierarchies that are still lightly coupled and This allows us to manage entities that have multiple orthogonal or orthogonal dimensions meaning there's an abstract hierarchy part of it and there's an implementation part of it and each of these can be extended separately independent of each other and We saw this point right here in the combo two of our radio and TV example where we can switch the implementation at runtime This is probably one of the most powerful aspects of the bridge pattern is that by using that pointer inside of our abstraction layer we can make it point to different implementations at runtime It implements the open closed principle by introducing new abstractions and implementations independently so we saw that because when we have the different top-level abstraction components and top-level implementation components we can Create new concrete subclasses as many as we want and that won't affect the previous behavior So it definitely manifests a lot of the solid principles that we've already learned and then as part of this one It creates platform independent systems So another way of seeing it is that we're hiding platform details. So if I go back to a previous diagram maybe I think this one would show it best so when our abstraction is pointing at this abstract implementation then the platform independent details in here don't really matter because we're calling specific functions in here in our interface That all the concrete classes will implement so from the shapes perspective It'll just blindly call whatever virtual functions are here and then Polymorphically it'll call the correct one at runtime All right, so some disadvantages So the first thing is that sometimes our code can get really complicated if we have one giant class, but it's giant and cohesive meaning it's doing its purpose and if we try to forcibly Rip apart that class into different hierarchies and abstractions and implementations it just might not work so sometimes a class is meant to be One unit and we shouldn't rip it apart if we don't have to And then related to that this idea of kind of separating a class out into different abstractions or implementation components is really vague and artificial and we kind of saw this with our color and shape example like I arbitrarily chose To separate out that concept where the shape is this high level component and The color is a low level component, but someone else can easily argue the opposite where the shape is the high level component Or so the color is the high level component and the shape is a low level component So it makes these kind of design decisions really difficult to understand when we try to do this