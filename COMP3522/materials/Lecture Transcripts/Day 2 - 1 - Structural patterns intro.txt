 Hey class, welcome to lecture today. Today we're going to be focused on structural patterns, specifically the proxy, facade, bridge, and decorator. And whenever we do talk about design patterns, I like to bring up this image just to remind us which category of design patterns we're talking about. So of course, there's three broad categories. There's behavioral, which is focused on communication between objects, creational, which is looking at different techniques to instantiate objects, and structural, which is the one we're looking at today, where we're looking at how we can combine and structure classes and objects to form entirely new structures in our program. So today we're going to be looking at four specific patterns, the proxy pattern when you want to wrap around an object and control access to it, the facade, where you want to provide the client with a very simple user interface that's going to hide the complexity every more complex system. There's the bridge pattern, which we use when we want to break down a large class into some smaller classes and design them in a way where we're talking about abstractions and implementations. And then there's the decorator pattern, where we're allowed to dynamically add functionality to an object at runtime.