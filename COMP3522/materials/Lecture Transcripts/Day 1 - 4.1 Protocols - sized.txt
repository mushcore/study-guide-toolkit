 A while ago I talked about protocols and I realized that back then I described the concept to you but I didn't give you solid code examples so this is why I'm going back to that topic and reviewing it a little bit. So just as a reminder protocols are related to interfaces and interfaces are pretty much a contract that says that a class must implement certain methods and behaviors or in other words in general OOP terms an interface refers to the public methods and attributes that are provided by a class and this determines how we can interact with that object and in Python generally there's two different kinds of interfaces there are informal interfaces which means that there's no specific syntax that stating that something does something and this is with duct typing and protocols which is what we're going to be looking at now specifically protocols and then there's the more formal interfaces where there is a syntax that's specifically stating that you're implementing something and that's done through abstract based classes but let's look at the Python protocol once again. So all protocols are is a collection of methods that Python developers and libraries expect certain kinds of things to have and this is analogous to interfaces in the Java collections framework and there's a whole bunch of very helpful protocols that we've been using so far and what we're going to be learning right now is how we can implement these protocols ourselves and the simplest one is the sized protocol and as a name implies this is a function we use to determine how length or how many elements are in your your custom object and the way that it's invoked is whenever we call it the length function so le and and the way that we implement it in our code is we have to override the dunder length dunder function and so let's say globally Python probably calls our custom dunder length function something like this there's some global le and function it takes in some object and then in that global code it probably just calls the object and it blindly calls the dunder length dunder function in it and this will typically returns the length of an object and it's an integer that's greater than or equal to zero so sometimes some examples you might have seen this are strings lists ranges two pulls bytes sets and dictionaries so let's look at a specific example on how we can implement it ourselves and we're going to check out this team sized code so here we have our team sized code and what it is is we're making a custom class called team and in this team I have a team name and a list of members in my team so in our initializer here we're taking in self which we always do for all the methods in our classes we have a team name and team numbers and the idea is that team name is going to be some string and team members is going to be a list of strings and just to make things easier for us to see we're implementing our pretty much our Python two string method which is the dunder str dunder and we just want to format our string of of team members properly so we're creating a team list which is an empty empty string and then we have a for loop that's going to iterate over every single string in our list of members list and then it's going to append their names to this team list and then finally we return our nicely formatted string in this specific format so it's going to say team the name of our team has these members and then it's going to print out all the the strings in our list of list of team names and then so notice the whole point of this is I want to show you how we can implement the size protocol and the way that we do that is we implement this dunder length dunder function so again this is whenever you see these dunders that means it's a built-in function in Python that we're just overwriting so specifically here we are overwriting our length function so this is a built-in function and we just happen to be overwriting it here so we have def dunder length dunder and we take in self as a parameter and the whole purpose of this is Python doesn't know how to interpret the size of our custom team team class so we're just saying okay whenever you call the length function on our team class I want to just show the number members in our team list or sorry I want to get the number of members in this a members list so you see it's very simple I just get my members list and then I call the length function on that list and then I return that value back out so let's bring it all together and then in my main function I'm going to be creating my custom team object it's going to be called the x men and then that's going to be passed as the first parameter and as a second parameter for my list of strings I just have a list of names for different members in my team it's a professor Gusevier, Cyclops, Storm and Gene and I save that into my xmen object so this is my team object and then I printed out so when I call print on my xmen that's going to be calling this string method and then here when I call the length function on my xmen object directly this will automatically call my sized protocol that's implemented here so let's give it a run and see what we get so when we run this and we print out xmen we see it automatically calls our string function right here which is pretty much our two string so I've printed out in this nicely written out format and then when I print out this one right here and call our sized protocol with the length xmen we see that it's printing out this code right here so this is length and then self dot members so this we know it's four because there's four strings in our list right here so that list of strings is saved inside of self dot members so when we call length on the list that's calling the size protocol on this list and it returns four so that's why this returns number four and then this gets a value for so let's say I did not implement the size protocol so in that case let's say I just comment out this one if I comment out this one that means I did not implement the size protocol so let's just see what happens so we didn't change any code here let's just run this and you see right away we get an error because team has no link function that's built