 So you would want to use the chain of responsibility when your program wants to process information in different ways. So either you know your processing order ahead of time or the opposite. If you don't know the sequence in which you want to process your request, then that's also possible. And you can even set this processing order at runtime. And that's impossible with the old approach. Another benefit of the chain of responsibility is it follows a single responsibility principle. Each handler is responsible for one type of processing. So this allows us to decouple the classes that invoke the operations or or call the handlers. So in our example, that was the enrollment system. We separate that out from the classes that perform the verification operations on a request. And then finally, it also follows the open close principle. So whenever you want to introduce a new handler, all you have to do is extend an existing base handler and then add new functionality to that. In terms of disadvantages, once again, very common for all these patterns is we create a lot of classes. So there's more classes to maintain. And some requests might actually end up being unhandled. So depending on the order in which you call your processing, if if one of your handles handlers fails to do the processing, it might return prematurely. So it was very important to set up your handlers in the proper order.